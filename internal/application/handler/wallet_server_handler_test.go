package handler_test

/*func TestUpdate(t *testing.T) {
	//Todo: URL ile al
	ginContext, _ := gin.CreateTestContext(httptest.NewRecorder())
	testTable := struct {
		server           *gin.Context
		expectedResponse ApplicationError
		expectedError    error
	}{
		server:           ginContext,
		expectedResponse: SuccessInUpdate(),
		expectedError:    nil,
	}

	testTable.server.JSON(http.StatusOK, dto.WalletDto{Balance: 1000})
	testTable.server.Set("user", dto.TokenUserDto{Id: 1})
	//Database den unite başla
}*/
